# Session Note — 2026-04-26

**Purpose:** Sync 15. **Universal URL Harvester ships.** 17-commit ground-up replacement of the per-aggregator extractor stack with a single-entry 3-tier cascade harvester that handles SPA aggregators, sensitive-content gates, and arbitrary interstitial pages.

**Branch / PR:** `phase-2-discovery-v2` → PR [#4](https://github.com/tommy811/The-Hub/pull/4) — head advanced from `530401d` (sync 14 close) through `f772add`.

---

## What We Did

### Universal URL Harvester (the whole session)

**Architecture:** new `scripts/harvester/` package, single entry `harvest_urls(url, supabase) -> list[HarvestedUrl]`. 3-tier cascade:

1. **Cache** (`harvester/cache.py`) — new `url_harvest_cache` table (workspace-agnostic, 24h TTL, mirrors the `classifier_llm_guesses` no-RLS pattern). On cache hit, returns immediately and skips Tiers 1+2.
2. **Tier 1** (`harvester/tier1_static.py`) — httpx + BeautifulSoup. Pulls the page, extracts anchors, runs a 4-signal escalation detector: SPA marker (astro-island, next-data, single-div pattern), near-empty anchor count (`< 3` resolved hrefs), sensitive-content gate keywords ("over 18", "i agree", "open link", etc.), JS-only body (no rendered text). Any signal trips → escalate.
3. **Tier 2** (`harvester/tier2_headless.py` + `harvester/page_function.js`) — `apify/puppeteer-scraper` actor. The page function:
   - Hooks `window.open` and `location.href` setters BEFORE page scripts execute (so SPA single-page redirectors get captured even if their click handler tries to navigate the entire window).
   - Auto-clicks 7 interstitial keyword variants ("open link", "continue", "i am over 18", "i agree", "i confirm", "18+", "enter") via Puppeteer 22+ `page.$$('xpath/...')` selector syntax.
   - Returns the unified anchor + intercepted-navigation set.

After harvest, each URL is canonicalized + classified via the existing `pipeline.classifier`, then mapped through a host-aware `_destination_class_for(account_type, canonical_url)` that promotes Substack subdomains / Spotify / Apple Podcasts → `content`; amzn.to / geni.us / lnk.to / shareasale → `affiliate`; Shopify subdomains / Etsy / Depop → `commerce`; Telegram / WhatsApp / Discord → `messaging`. Same-host self-links are dropped.

**Schema changes (4 migrations applied via Supabase MCP):**
- `20260426000000_url_harvester_v1` — adds `url_harvest_cache` table (23 → 24 tables); adds 3 audit columns to `profile_destination_links` (`harvest_method`, `raw_text`, `harvested_at`).
- `20260426010000_commit_discovery_result_v3_harvester_audit` — RPC writes the new audit fields.
- `20260426020000_extend_destination_class_check` — extends the CHECK constraint from 4 → 10 values (`monetization|aggregator|social|commerce|messaging|content|affiliate|professional|other|unknown`). TEXT-with-CHECK pattern, not Postgres ENUM.
- `20260426030000_commit_discovery_result_v4_update_destination_class` — patches the ON CONFLICT clause to update `destination_class` so post-fix re-runs refresh stale rows.

**Resolver wiring** — `pipeline/resolver.py::_classify_and_enrich` now delegates to `harvest_urls()` (one call per page returns all outbound destinations) instead of dispatching per-aggregator. Deleted `scripts/aggregators/` entirely.

**UI** — Creator HQ renders new "All Destinations" section grouped by `destination_class` with a `gated` chip on rows where `harvest_method='headless'` (so you can spot at a glance which destinations required the Tier 2 escalation).

**Gazetteer** — ~30 new rules in `data/monetization_overlay.yaml` covering BuyMeCoffee, Ko-fi, Substack subdomains, Spotify, Telegram, WhatsApp, Apple Podcasts, affiliate redirectors.

**Canonicalizer extension** — strips additional tracking params (`igsh`, `l_`, `s`, `_t`, `aff`, `ref_id`) so `?s=21` and `?igsh=...` no longer create false-distinct rows in `profile_destination_links`.

### Live smoke (the proof)

Re-discovered `esmaecursed-1776896975319784`. The previously-invisible **Fanplace link** — hidden behind `tapforallmylinks.com`'s 2-step "Sensitive Content / Open link" gate — was cleanly captured. Tier 1 detected the gate via the sensitive-content signal; Tier 2 hooked the `location.href` setter and harvested all 6 destinations including 2 messaging links. All correctly classified.

Total Apify spend ~80¢ across 4 smoke runs (the Puppeteer Scraper actor is more expensive per-run than the lightweight scrapers, but the cache makes re-runs cheap).

### Inline regression catches

- **Puppeteer 22+ migration** — `page.$x` was deprecated; switched to `page.$$('xpath/...')` in `page_function.js` (commit `b632b81`).
- **Messaging class mapping** — Telegram URLs were initially landing as `other`; added the messaging mapping to the host-aware promoter (same commit).
- **Same-host self-link filter** — an aggregator's footer linking to its own homepage was surfacing as a destination of itself. Filter added to the orchestrator (same commit).
- **CHECK constraint extension** caught mid-smoke when `messaging` rows started crashing on insert (migration `20260426020000`).
- **ON CONFLICT updates `destination_class`** caught mid-smoke when re-running discovery on a creator with cached destination rows didn't refresh their class (migration `20260426030000`).

---

## Key Decisions

- **Single universal entry, not per-aggregator extractors.** The old `aggregators/{linktree,beacons,custom_domain}.py` model didn't generalize to novel aggregator platforms (link.me, allmylinks, tapforallmylinks, custom Astro pages). One `harvest_urls()` call handles every page and the 3-tier cascade decides cost. Deleted the entire `aggregators/` package.
- **TEXT + CHECK, not Postgres ENUM, for `destination_class`.** Forward-compat additions are a single `DROP CONSTRAINT / ADD CONSTRAINT` swap instead of `ALTER TYPE ... ADD VALUE` ceremony. Worth it given we extended 4 → 10 in this session and likely will again.
- **Page function hooks `window.open` + `location.href` BEFORE page scripts run.** Many sensitive-content gates do `location.href = '...'` instead of opening an anchor, so a passive crawler misses them entirely. Hooking BEFORE scripts execute is the only reliable capture.
- **Auto-click 7 interstitial keyword variants.** Empirically derived from the live smoke (the OnlyFans-adjacent corner of the web is full of these gates). `page.$$('xpath/...')` selector syntax works in Puppeteer 22+.
- **Cache TTL = 24h, workspace-agnostic.** Mirrors `classifier_llm_guesses`. Aggregator pages don't change minute-to-minute and the dedup wins (re-running discovery on a known creator is now ~free for the harvester layer).
- **`gated` chip in UI**, not a hard error. A `harvest_method='headless'` row is a signal that the destination required active interception — useful UX for "is this a real CTA the creator owns, or did we follow a hostile gate". Not blocking.
- **Did not modify `scripts/.env`.** `SUPABASE_DB_URL` is still unfilled and SCHEMA.md regen still blocks on it. PROJECT_STATE.md §4.1 hand-updated to 24 tables; cosmetic discrepancy with `docs/SCHEMA.md`. Simon's call.

---

## Current Status

- **Tests:** 192 pytest passing. tsc 0 errors.
- **Branch:** `phase-2-discovery-v2`. **17 commits ahead of origin** going into this sync; the sync commit + push will catch the remote up.
- **Schema:** 24 tables live in Supabase. `docs/SCHEMA.md` shows 23 (regen blocked). PROJECT_STATE.md §4.1 reflects reality.
- **Live smoke:** confirmed Fanplace link capture on `esmaecursed-1776896975319784`. ~80¢ total Apify spend.

---

## Next Session

- **Tier 2 cost optimization.** Each gated page = ~2¢ of `apify/puppeteer-scraper` time. Some pages don't need full headless — could the 4-signal escalation detector be tuned to escalate less aggressively? Specifically: SPA marker is currently a hard escalate; some Astro pages render real anchors that BeautifulSoup picks up cleanly. Could measure false-escalate rate against a fixture set.
- **Host-gate `s` and `t` tracking-param strips to twitter.com only.** Currently the canonicalizer strips them broadly, which is correct for IG (`?igsh=...`) and Twitter (`?s=21`) but might over-strip on hosts where those params are meaningful. Audit the deduped rows for false positives.
- **SCHEMA.md regen.** Simon needs to fill `SUPABASE_DB_URL` in `scripts/.env` (host visible in Supabase project settings; password is the DB password, not the service role key), then run `npm run db:schema && npm run db:types`. Documented in user memory `project_schema_tooling.md`.
- **Highlights v1 thaw decision** still pending from Simon (carryover from sync 14). Recursive funnel + Universal URL Harvester now catch the majority case; highlights gap remains the slice of OF-adjacent creators who park CTAs only in IG highlights. Decision: add IG sessionCookies vs. defer indefinitely.

---

## Files Changed (this session — 17 commits)

1. `6fe423e` — `feat(canonicalize): strip igsh/l_/s/_t/aff/ref_id tracking params`
2. `4cbd65b` — `feat(harvester): add HarvestedUrl/Tier1Result types + extend DestinationClass to 10 values`
3. `df456c1` — `feat(db): url_harvester_v1 — url_harvest_cache + profile_destination_links audit cols`
4. `f443933` — `feat(harvester): URL cache module — 24h TTL, supabase-py 2.x None-safe`
5. `584302e` — `feat(harvester): Tier 1 static fetcher with 4-signal escalation detector`
6. `f143e64` — `fix(harvester): refocus tapforallmylinks test on contract (Tier 2 escalation) + add astro-island SPA marker`
7. `7ba5e5d` — `feat(harvester): Tier 2 headless harvester via apify/puppeteer-scraper`
8. `b529ee0` — `fix(harvester): full A-Z translate mask + add 3 interstitial keywords + log actor failures + drop deprecated waitForTimeout`
9. `321e651` — `feat(harvester): orchestrator cascade — cache → tier1 → tier2 → classify`
10. `c10dc15` — `feat(harvester): extend gazetteer with ~30 rules + host-aware destination_class mapping`
11. `27d7ad5` — `refactor(resolver): replace per-aggregator dispatch with harvest_urls()`
12. `43e820a` — `feat(commit_v2): write harvest_method + raw_text to profile_destination_links`
13. `c71fd2e` — `refactor: remove deprecated aggregators package — replaced by scripts/harvester/`
14. `880fadf` — `feat(creator-hq): All Destinations section grouped by destination_class`
15. `b632b81` — `fix(harvester): page.$x→page.$$ (Puppeteer 22+) + messaging class mapping + same-host filter`
16. `6edbee6` — `fix(db): extend destination_class CHECK constraint to accept all 10 values`
17. `f772add` — `fix(commit_v2): ON CONFLICT now updates destination_class so post-fix re-runs refresh stale rows`

Plus the sync commit (this one).

---

## Additional session — sync 16 (T16 + T17)

**Purpose:** Two task batches landed in one session: T16 (profile noise filter retroactive cleanup + resolver/commit_v2 dedup fixes + UI polish) and T17 (specific platform identification + AccountRow restructure + banner foundation). 8 commits ahead of origin pre-sync.

**Branch / PR:** `phase-2-discovery-v2` → PR [#4](https://github.com/tommy811/The-Hub/pull/4) — head advanced from `f772add` (sync 15 close) through `c08dcdd`.

---

### What We Did

**T16 — Profile noise cleanup retroactive + resolver/commit_v2 dedup + UI polish (commit `23cc1e9`)**

- **Soft-deleted 30 stale noise profile rows** across all 5 creators in one sweep. Patterns: `*.api.linkme.global` (link.me API redirector hosts that classified as profiles), `*.cloudfront.net` (CDN), `*.page.link` (Firebase Dynamic Links shortener), empty-path homepages (`instagram.com/` no handle), legal/footer paths (`/terms`, `/privacy`, `/about`), plus a Phase-1.2 follow-up sweep for `music.link.me`.
- **Resolver `_classify_and_enrich` now calls `is_noise_url(canon)`** immediately after `visited_canonical.add(canon)` — drops noise URLs at the gate, before they ever become `DiscoveredUrl` rows. Forward fix complementing the retroactive cleanup.
- **`_commit_v2` URL-keyed dedup pass on `accounts`** before the RPC call — higher `discovery_confidence` wins; non-`other` platform wins on tie. Resolves Aria's `tapforallmylinks` duplicate-row pattern.
- **PLATFORMS dict + `getPlatformFromUrl()` + `resolvePlatform(platform, url)`** — added `reddit, threads, bluesky, snapchat` with proper Si* icons; URL-host inference fallback for legacy `platform='other'` data.
- **Unified clip icon (`lucide Link`)** for all aggregator types (linktree/beacons/custom_domain). Visual consistency across the AccountRow surface.
- **All Destinations panel moved BELOW tabs** wrapped in native collapsed-by-default `<details>` element. Reduces visual noise above the fold.
- **Stats Strip uses `resolvePlatform(platform, url)`** so `Other` rows display proper labels (Reddit/Snapchat); Link-in-Bio shows platform names instead of raw handles.

**T17 — Specific platform identification + AccountRow restructure + banner foundation (commit `c08dcdd`)**

- **Postgres `platform` enum extended +19 values** via migration `20260426040000_add_platform_values_specific_aggregators_and_monetization`: `link_me`, `tapforallmylinks`, `allmylinks`, `lnk_bio`, `snipfeed`, `launchyoursocials`, `fanfix`, `cashapp`, `venmo`, `snapchat`, `reddit`, `spotify`, `threads`, `bluesky`, `kofi`, `buymeacoffee`, `substack`, `discord`, `whatsapp`. **Total enum count now ~37 values.**
- **Pydantic `Platform` Literal in `scripts/schemas.py`** extended to match. Drift caught in-flight when the implementer's first discovery run after the migration failed pre-commit on `pydantic.ValidationError` — fixed before push. `test_schemas.py` updated.
- **Gazetteer rewritten** — 13 new specific host→platform rules in `data/monetization_overlay.yaml`; 6 older generic rules removed so new specifics win. Examples: `tapforallmylinks.com → tapforallmylinks` (was `custom_domain`); `cash.app → cashapp` (was `other`); `link.me → link_me` (was `custom_domain`); `app.fanfix.io → fanfix` (was `other`); `venmo.com → venmo` (was `other`).
- **`src/lib/platforms.ts` PLATFORMS dict** gained 13+ new entries with proper Si* icons (Spotify/Substack/Discord/WhatsApp/Kofi/BuyMeACoffee verified present in react-icons 5.6.0). Cash App / Venmo / Fanfix use lucide fallbacks (`DollarSign`, `Heart`). No new dependencies.
- **`resolvePlatform` derives URL hostname as label** when `platform='custom_domain'` and no specific aggregator matches → custom funnel domains show as e.g. "ariaswan.com" instead of "Custom Domain". `HOST_PLATFORM_MAP` extended with all new aggregator/monetization hosts so URL-host inference works for legacy data.
- **AccountRow layout restructured** per Simon's UX call: Column 1 (180px) = `[icon] [Platform Name]`. Column 2 (280px) = handle (clickable) + display_name secondary line. Platform name first, not handle first.
- **11 backfill UPDATEs** converted existing profile rows from `other` / `custom_domain` to specific platforms (tapforallmylinks=3, link_me=1, fanfix=1, cashapp=1, venmo=1, snapchat=2, reddit=1, spotify=1).
- **Migration `20260426050000_creator_cover_and_banner.sql`** added 3 new columns to `creators`: `cover_image_url` (scraper-set, null pending Phase 3), `banner_url` (agency-managed override), `override_avatar_url` (agency-managed headshot).
- **New `<BannerWithFallback>` client component** rendered on creator HQ below the merge-candidate banner / above the header. Both creator HQ and grid card prefer `override_avatar_url` over scraper avatar.
- **All 5 creators re-discovered cleanly** (Esmae, Aria, Kira, Natalie, Valentina). ~$1 Apify spend across 5 runs. Per-creator results validated:
  - Aria → `tapforallmylinks` + Fanplace (with proper labels)
  - Valentina → `link.me` + Fanfix + Cash App + Venmo coexist as 4 separate rows (the unique-key collision bug is fixed)
  - Kira → `link.me` + Linktree
  - Natalie → 2 Telegram channels survived alongside 2 `tapforallmylinks` rows + Fanplace
  - Esmae → `tapforallmylinks`×2 + Fanplace + Telegram×2

---

### Key Decisions

- **Specific platform values over generic 'other' to fix unique-key collisions.** Three options were on the table: (a) Postgres enum extension with specific values per aggregator/monetization platform; (b) host-prefix-handle synthetic uniqueness (e.g. `handle = "tapforallmylinks.com_esmaecursed"`); (c) change the `(creator_id, platform, profile_url)` unique constraint shape entirely. Picked (a) — cleanest. Distinct URLs from distinct platforms remain distinct rows naturally; gazetteer specificity guarantees the right classification at insertion time. (b) was uglier; (c) was riskier.
- **Platform name first in AccountRow, not handle first** (Simon's UX call). The previous "handle first" layout made the page feel handle-centric; Simon wanted the platform identity to lead so the row reads as "this creator has a Reddit account" rather than "this creator has @whatever". Column 1 = `[icon] [Platform Name]`, Column 2 = handle + display_name secondary line.
- **Banner UI minimal pending Phase 3 scraper work.** Migration adds the 3 columns and `<BannerWithFallback>` renders them, but `cover_image_url` stays null until a Phase 3 scraper populates it (FB cover photo / Twitter banner / Reddit banner / etc.). `banner_url` and `override_avatar_url` are agency-managed and can be set manually now via direct UPDATE. Holding the structural ground for Phase 3 without gold-plating the surface.
- **Pydantic-vs-Postgres enum drift caught at pre-commit, escalated to future-work CI test.** The implementer's first discovery run after the enum extension failed with `pydantic.ValidationError` because `scripts/schemas.py::Platform` Literal hadn't been updated. Fixed in the same commit. Future enum extensions could silently break discovery without a CI diff test — flagged as future work in PROJECT_STATE §20 Known Limitations.
- **Profile noise filter applied retroactively AND at the resolver gate.** Sweep deleted 30 existing rows; `is_noise_url(canon)` predicate now blocks the same patterns at `_classify_and_enrich` entry. Belt-and-suspenders: existing data cleaned, new data prevented from accumulating the same way.

---

### Current Status

- **Tests:** 227 pytest passing. tsc 0 errors.
- **Branch:** `phase-2-discovery-v2`. **8 commits ahead of origin** going into this sync; the sync commit + push will catch the remote up.
- **Schema:** 24 tables live in Supabase (unchanged from sync 15). `creators` table gained 3 nullable text columns. `platform` enum extended from 18 → 37 values.
- **Live smoke:** all 5 creators (Esmae, Aria, Kira, Natalie, Valentina) re-discovered cleanly with specific platform labels. ~$1 total Apify spend.

---

### Next Session

- **Pydantic-vs-Postgres enum drift CI test.** Diff `scripts/schemas.py::Platform.__args__` against `SELECT enumlabel FROM pg_enum WHERE enumtypid = 'platform'::regtype`; fail the build on mismatch. Easy to write; would have caught the T17 in-flight breakage at PR time instead of pre-commit panic.
- **`visit.link.me/.../sensitiveLinkLabel=OF` redirector exception.** The eTLD+1 same-host filter (introduced sync 15) over-eagerly drops link.me redirector URLs whose query string carries telling markers. Option (b) from sync 15 — keep URLs with telling query markers — still not implemented.
- **Phase 3 banner scraper.** FB / Twitter / Reddit profile fetchers should write `cover_image_url` alongside `avatar_url`. Until then, banners only appear when an agency operator manually sets `banner_url`.
- **Clean Esmae's stale `handle="tapforallmylinks.com"` host-as-handle row.** One-off `UPDATE profiles SET handle = 'esmaecursed' WHERE id = ...` next time someone is in the DB. Pre-T17 row from older data; non-canonical clutter; non-urgent.
- **Apify memory cap watch.** Kira's TT enrichment hit the 4096MB cap during T17 re-run (one-off). If it recurs, request the 8GB tier on actor input or reduce per-run scope.

---

### Files Changed (this section — 2 commit batches)

1. `23cc1e9` — `fix(ui+pipeline): noise filter at profile creation, dedup same-URL accounts, Reddit/Threads/Bluesky/Snapchat icons, unified aggregator clip icon, foldable All Destinations at bottom, accurate Stats Strip` — T16 batch.
2. `c08dcdd` — `feat(platforms): T17 — specific platform identification, AccountRow layout, banner foundation` — T17 batch.

Plus the sync commit (this one).
