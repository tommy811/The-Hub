# 2026-04-28

## Sync 20 — 90-day scrape + filtered content surfaces

- Ran the manual IG/TT content scraper for all 18 active profiles in workspace `00000000-0000-0000-0000-000000000010` over 90 days: 13 profiles scraped, 5 skipped as `no_posts`, 643 posts upserted, 0 failures.
- Post-run verification found 639 current 90-day rows: 345 Instagram, 294 TikTok, 75 outliers, 528 posts with audio signatures.
- Linked repeated platform audio via `scripts/extract_audio_trends.py --min-usage 2`: 49 audio trend rows, 137 scraped posts linked by `trend_id`.
- Product routing corrected: `/content` is reserved for agency creation tools; scraped posts live at `/scraped-content`.
- Added shared content filters for scope, platform, search, sort, min outlier multiplier, audio presence, repeat-audio trend status, and review status.
- Opened live IG/TT Outliers pages with filtering and audio/trend columns.
- Replaced `/trends` mock with live Audio Trends table and links back into filtered scraped content by audio signature.

## Notes

- `supabase/migrations/20260427000200_scrape_runs_observability.sql` is committed but not applied live. The live scrape returned `PGRST205` for `public.scrape_runs`, and the scraper correctly treated that logging failure as non-fatal.
- Verification: `npm run typecheck` passed; `npm run lint` passed with 2 pre-existing creator detail warnings; content quality/trend pytest passed; route smoke checks returned 200 for `/scraped-content`, both outlier pages, and `/trends`.

## Sync 21 — winning-content triage + avatars + IG highlights

- Added the winning-content follow-up plan and implemented Phase 2-safe sorting/filtering: copy priority, format, known/missing views, pinned/sponsored, likes/comments/shares/saves, min creators for audio trends, and repeat-audio creator counts.
- Refreshed live profile photos with limited Apify profile-only/one-post fetches: 16/18 active IG/TT accounts now have `profiles.avatar_url`; still missing IG `kirapregiato_backup` and IG `ellableu.fit`.
- Added `/api/avatar` plus allowed CDN host checks so platform avatar URLs are proxied through the app.
- Confirmed Supabase previously had `0` IG `story` / `story_highlight` rows.
- Re-tested official `apify/instagram-scraper resultsType=stories`; it returned normal feed/reel records for `@esmaecursed`, so it remains unsuitable for highlights.
- Ran dedicated `seemuapps/instagram-highlights-scraper` once for current IG profiles and imported the already-paid dataset `6XHq1bewmAcEjDVfc` through `scripts/scrape_instagram_highlights.py`: 139 `story_highlight` rows across 5 IG profiles.
- Current live scraped content count: 788 rows (`image` 24, `carousel` 85, `reel` 245, `tiktok_video` 295, `story_highlight` 139).
- Verification: targeted tests 37/37, full Python tests 302/302 + 1 skip, `npm run typecheck`, `npm run lint`, `npm run build`, localhost route smokes 200 for `/`, `/creators`, `/platforms/instagram/accounts`, `/scraped-content`.
